import React from "react"
import styled from "styled-components"

const styledContainer = styled.div`
  background-color: red;
  
`

const Footer = () => {
  return <styledContainer>H</styledContainer>
}

export default Footer
